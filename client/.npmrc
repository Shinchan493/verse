# react-scripts 5 only peer-accepts TypeScript 3/4, but this project uses
# TypeScript 5 (required by tldraw). Use legacy peer resolution so installs
# don't fail with ERESOLVE on CI / Render.
legacy-peer-deps=true
